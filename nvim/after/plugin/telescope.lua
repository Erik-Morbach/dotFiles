require('telescope').setup({
    defaults = {
        layout_config = {
            vertical = { width = 0.5 },
        },
    },
    pickers = {
        diagnostics = {
            theme = "ivy",
        },
    }
})
